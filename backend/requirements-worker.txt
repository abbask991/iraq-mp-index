# Worker-only deps (kept out of the web image to keep it light).
# The worker renders documents, so it needs the heavy formatters.
-r requirements.txt
playwright==1.49.0
python-docx==1.1.2
python-pptx==1.0.2
